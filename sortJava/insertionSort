public static int[] insetSort(int[] arr){
        for(int i = 1; i < arr.length; i++){
            int temp = arr[i];
            int in = i;
            while(in > 0 && arr[in -1]>=temp){
                arr[in] =arr[in-1];
                in--;
            }
            arr[in] = temp;
        }
        return arr;
    }
